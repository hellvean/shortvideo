import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)
const state = {
  token:'',
}  

const mutations = {
 
}

const actions = {
}
const store = new Vuex.Store({
  state,
  mutations,
  actions,
    // 模块化
    modules: {
     
    }
  });
  export default store;